VALUE_2 = 2

def helper_2_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 0
    return max(0, scaled)

def helper_2_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 1
    return max(0, scaled)

def helper_2_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 2
    return max(0, scaled)

def helper_2_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 3
    return max(0, scaled)

def helper_2_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 4
    return max(0, scaled)

def helper_2_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 5
    return max(0, scaled)

def helper_2_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 6
    return max(0, scaled)

def helper_2_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_2 + 7
    return max(0, scaled)
