VALUE_3 = 3

def helper_3_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 0
    return max(0, scaled)

def helper_3_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 1
    return max(0, scaled)

def helper_3_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 2
    return max(0, scaled)

def helper_3_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 3
    return max(0, scaled)

def helper_3_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 4
    return max(0, scaled)

def helper_3_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 5
    return max(0, scaled)

def helper_3_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 6
    return max(0, scaled)

def helper_3_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_3 + 7
    return max(0, scaled)
