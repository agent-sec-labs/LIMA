VALUE_4 = 4

def helper_4_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 0
    return max(0, scaled)

def helper_4_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 1
    return max(0, scaled)

def helper_4_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 2
    return max(0, scaled)

def helper_4_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 3
    return max(0, scaled)

def helper_4_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 4
    return max(0, scaled)

def helper_4_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 5
    return max(0, scaled)

def helper_4_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 6
    return max(0, scaled)

def helper_4_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_4 + 7
    return max(0, scaled)
