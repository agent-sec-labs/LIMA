VALUE_13 = 13

def helper_13_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 0
    return max(0, scaled)

def helper_13_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 1
    return max(0, scaled)

def helper_13_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 2
    return max(0, scaled)

def helper_13_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 3
    return max(0, scaled)

def helper_13_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 4
    return max(0, scaled)

def helper_13_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 5
    return max(0, scaled)

def helper_13_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 6
    return max(0, scaled)

def helper_13_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_13 + 7
    return max(0, scaled)
