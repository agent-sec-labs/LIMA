VALUE_14 = 14

def helper_14_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 0
    return max(0, scaled)

def helper_14_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 1
    return max(0, scaled)

def helper_14_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 2
    return max(0, scaled)

def helper_14_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 3
    return max(0, scaled)

def helper_14_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 4
    return max(0, scaled)

def helper_14_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 5
    return max(0, scaled)

def helper_14_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 6
    return max(0, scaled)

def helper_14_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_14 + 7
    return max(0, scaled)
