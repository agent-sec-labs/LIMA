VALUE_120 = 120

def helper_120_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 0
    return max(0, scaled)

def helper_120_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 1
    return max(0, scaled)

def helper_120_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 2
    return max(0, scaled)

def helper_120_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 3
    return max(0, scaled)

def helper_120_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 4
    return max(0, scaled)

def helper_120_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 5
    return max(0, scaled)

def helper_120_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 6
    return max(0, scaled)

def helper_120_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_120 + 7
    return max(0, scaled)
