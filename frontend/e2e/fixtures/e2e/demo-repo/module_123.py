VALUE_123 = 123

def helper_123_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 0
    return max(0, scaled)

def helper_123_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 1
    return max(0, scaled)

def helper_123_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 2
    return max(0, scaled)

def helper_123_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 3
    return max(0, scaled)

def helper_123_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 4
    return max(0, scaled)

def helper_123_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 5
    return max(0, scaled)

def helper_123_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 6
    return max(0, scaled)

def helper_123_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_123 + 7
    return max(0, scaled)
