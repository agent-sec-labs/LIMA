VALUE_125 = 125

def helper_125_0(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 0
    return max(0, scaled)

def helper_125_1(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 1
    return max(0, scaled)

def helper_125_2(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 2
    return max(0, scaled)

def helper_125_3(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 3
    return max(0, scaled)

def helper_125_4(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 4
    return max(0, scaled)

def helper_125_5(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 5
    return max(0, scaled)

def helper_125_6(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 6
    return max(0, scaled)

def helper_125_7(amount):
    """Deterministic filler used only to size the E2E fixture."""
    scaled = amount + VALUE_125 + 7
    return max(0, scaled)
